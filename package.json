{
  "name": "airline-points-search",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:worker": "npm run dev --workspace=apps/scraper-worker",
    "build": "npm run build --workspace=apps/web && npm run build --workspace=apps/scraper-worker",
    "typecheck": "npm run typecheck --workspace=packages/shared && npm run typecheck --workspace=apps/web && npm run typecheck --workspace=apps/scraper-worker",
    "db:migrate": "node scripts/migrate.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
